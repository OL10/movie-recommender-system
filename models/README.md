# Models Directory

This folder stores trained model files.

## Generated Files

After running `python train.py`, this folder will contain:

- **hybrid_recommender.pkl** - Trained hybrid recommendation model (TF-IDF + SVD)

## Note

Model files are not tracked in Git due to size. Run the training script to generate them locally.
